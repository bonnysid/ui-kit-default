export const PERSISTED_STATE_VERSION = '1';

export const PERSISTED_STATE_EVENT_PREFIX = `use-persisted-state-v${PERSISTED_STATE_VERSION}`;

export const UNSET = Symbol('UNSET');

export const READ_ERROR = Symbol('READ_ERROR');
